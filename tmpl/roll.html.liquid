<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>📷 {{ roll_number }} - {{ camera }} {{ film }}</title>
    <style>
      body {
        font-family: ia-writer-quattro, ui-monospace, SFMono-Regular, Menlo, Monaco, monospace;
      }
      section {
        margin-left: 30px;
        min-width: 50vw;
        max-width: 70vw;
      }
      img {
        object-fit: cover;
        width: 100%;
      }
      h1, h2 {
        margin-bottom: 0;
        margin-top: 8px;
      }
      header {
        margin: 30px 0;
      }
      .lists {
        font-style: italic;
      }
      .bold {
        font-weight: 600;
      }
		</style>
  </head>
	<body class="container ml-8">
		<section class="flex flex-col py-4" id="roll_{{ roll_number }}">
      <header>
        <span class="roll_number" >#{{ roll_number }} - {{ scanned_at }}</span>
        <h1 class="text-xl font-light font-serif">{{ camera }} - {{ film }}</h1>
      </header>
      <dl class="lists">
        <dt class="bold">Tags</dt>
        <dd>{% for tag in tags %} {{ tag }}, {% endfor %}</dd>
        <dt class="bold">Places</dt>
        <dd>{% for place in places %} {{ place }}, {% endfor %}</dd>
      </dl>
			<div class="grid">
					<img src="{{ roll_number }}.jpg" />
			</div>
      <p>
      {{ content }}
      </p>
		</section>
	</body>
</html>
